- if user_signed_in?
  %div
    = simple_form_for @timeline do |f|
      = f.input_field :content, label: '投稿内容(最大140字)', class: 'form-control'
      = f.file_field :attached_image_path, label: '添付画像', id: :attached_image_path
      = f.submit '送信', class: 'btn btn-primary'
